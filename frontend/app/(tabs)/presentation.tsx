// This file exists so Expo Router doesn't error on the route,
// but it's hidden from the tab bar (href: null in _layout.tsx).
// The actual presentation canvas is rendered inside the main screen.
export { default } from "./index";
